<% if signed_in? %>
  <%= javascript_tag do %>
    window.PUSHER_KEY = "<%= ENV.fetch("PUSHER_KEY") %>";
    window.PUSHER_CHANNEL_NAME = "<%= current_user.pusher_channel_name %>";
  <% end %>
<% end %>
