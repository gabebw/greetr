<% if signed_in? %>
  <%= javascript_tag do %>
    window.PUSHER_KEY = "<%= ENV.fetch("PUSHER_KEY") %>";
    window.PUSHER_CHANNEL_NAME = "<%= PusherClient.channel_name(current_user.id) %>";
  <% end %>
<% end %>
